const overviewUiActions = {

};

const initData = {

};

let overviewUiReducer = function (state=initData, {type}) {
	switch (type) {
		default:{
			break;
		}
	}
	return state;
};

export {overviewUiReducer, overviewUiActions};